# ChatNexus Deployment Architecture

## 📊 Complete Deployment Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          ChatNexus Deployment Options                        │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐
│  Your Code (GitHub)  │
│   ChatNexus Repo     │
└──────────────┬───────┘
               │
        git push origin main
               │
        ┌──────┴──────────────────────────────────────┐
        │                                             │
        ▼                                             ▼
   ┌─────────────┐                            ┌──────────────┐
   │ Railway.app │                            │  Render.com  │
   │ Auto Deploy │                            │ Auto Deploy  │
   └────┬────────┘                            └──────┬───────┘
        │                                           │
        ├─ Detect Dockerfile                       ├─ Detect Dockerfile
        ├─ Build Docker Image                      ├─ Build Docker Image
        ├─ Set Environment Vars                    ├─ Set Environment Vars
        ├─ Deploy Container                        ├─ Deploy Container
        └─────┬─────────────────────┬──────────────┘
              │                     │
              ▼                     ▼
       ┌─────────────────┐  ┌──────────────────┐
       │   MongoDB       │  │ MongoDB Atlas    │
       │   (Railway)     │  │ (External)       │
       └────────┬────────┘  └────────┬─────────┘
                │                    │
                └────────────┬───────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │  ChatNexus App   │
                    │  (Running in     │
                    │   Container)     │
                    │                  │
                    │ Port: 8080       │
                    └────────┬─────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
                    ▼                 ▼
              ┌──────────────┐  ┌────────────────┐
              │  Cloudinary  │  │  Health Check  │
              │  (Images)    │  │  /actuator/... │
              └──────────────┘  └────────────────┘
```

---

## 🏗️ Local Development Setup

```
┌───────────────────────────────────────────────────────────┐
│                  Local Development Machine                 │
├───────────────────────────────────────────────────────────┤
│                                                            │
│  ┌─────────────────────────────────────────────────────┐ │
│  │              Docker Compose Services               │ │
│  ├─────────────────────────────────────────────────────┤ │
│  │                                                     │ │
│  │  ┌────────────────────┐    ┌──────────────────┐   │ │
│  │  │ ChatNexus App      │    │ Local MongoDB    │   │ │
│  │  ├────────────────────┤    ├──────────────────┤   │ │
│  │  │ Port: 8080         │    │ Port: 27017      │   │ │
│  │  │ Java 17            │    │ Image: mongo:7.0 │   │ │
│  │  │ Spring Boot 4.0.1  │    │                  │   │ │
│  │  │ Environment: DEV_* │    │ Volume: db_data  │   │ │
│  │  └─────────┬──────────┘    └────────┬─────────┘   │ │
│  │            │ connects to           │              │ │
│  │            └───────────────────────┘              │ │
│  │                                                     │ │
│  └─────────────────────────────────────────────────────┘ │
│                       ▲                                   │
│                       │                                   │
│              docker-compose up -d                         │
│                       │                                   │
│            ┌──────────┴──────────┐                       │
│            │                     │                       │
│            ▼                     ▼                       │
│   ┌──────────────────┐  ┌──────────────────┐           │
│   │   .env file      │  │  docker-compose  │           │
│   │ (DEV variables)  │  │      .yml        │           │
│   └──────────────────┘  └──────────────────┘           │
│                                                         │
└───────────────────────────────────────────────────────────┘
```

---

## 🌐 Railway Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    Railway.app Environment                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  GitHub Repository                                          │
│         │                                                    │
│         │ (Webhook: git push detected)                      │
│         ▼                                                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           Railway Build Pipeline                     │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │ 1. Clone repository                                 │  │
│  │ 2. Read Dockerfile                                 │  │
│  │ 3. Build Docker image (maven build stage)          │  │
│  │ 4. Test image                                       │  │
│  │ 5. Push to Railway registry                         │  │
│  │ 6. Stop previous deployment                         │  │
│  │ 7. Start new container                              │  │
│  │ 8. Run health checks                                │  │
│  └────┬────────────────────────────────────────────────┘  │
│       │                                                     │
│       ▼                                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │   ChatNexus Container (Production)                   │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │ SPRING_PROFILES_ACTIVE=prod                         │  │
│  │ PROD_MONGO_URI=mongodb://...@mongodb.railway.internal
│  │ PROD_JWT_SECRET=***                                 │  │
│  │ Port: 8080 → Railway assigns public HTTPS port     │  │
│  └────┬─────────────────────────┬──────────────────────┘  │
│       │                         │                         │
│       ▼                         ▼                         │
│  ┌───────────────────┐   ┌────────────────────────────┐  │
│  │  MongoDB Service  │   │  Health Check Endpoint     │  │
│  │  (Internal URL)   │   │  /actuator/health          │  │
│  │                   │   │  Every 30 seconds          │  │
│  │ mongodb.railway   │   │                            │  │
│  │ .internal:27017   │   │  Expected: {"status":"UP"} │  │
│  └───────────────────┘   └────────────────────────────┘  │
│                                                              │
│  Public URL: https://chatnexus.up.railway.app              │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🎨 Render Deployment Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                    Render.com Environment                     │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  GitHub Repository                                           │
│         │                                                     │
│         │ (Webhook: git push detected)                       │
│         ▼                                                     │
│  ┌────────────────────────────────────────────────────────┐ │
│  │          Render Build Pipeline                        │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ 1. Clone repository                                  │ │
│  │ 2. Read Dockerfile                                  │ │
│  │ 3. Execute: docker build .                          │ │
│  │ 4. Multi-stage build:                               │ │
│  │    Stage 1: Maven compilation                       │ │
│  │    Stage 2: Runtime with JRE                        │ │
│  │ 5. Push to Render registry                          │ │
│  │ 6. Deploy new container                             │ │
│  │ 7. Monitor health checks                            │ │
│  └────┬─────────────────────────────────────────────────┘ │
│       │                                                     │
│       ▼                                                     │
│  ┌────────────────────────────────────────────────────────┐ │
│  │   ChatNexus Container (Production)                    │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ SPRING_PROFILES_ACTIVE=prod                          │ │
│  │ PROD_MONGO_URI=mongodb+srv://...@atlas.mongodb.net   │ │
│  │ PROD_JWT_SECRET=***                                  │ │
│  │ Port: 8080 → Render maps to HTTPS                    │ │
│  └────┬──────────────────────┬───────────────────────────┘ │
│       │                      │                             │
│       ▼                      ▼                             │
│  ┌──────────────────┐  ┌───────────────────────────────┐ │
│  │ MongoDB Atlas    │  │  Health Check                 │ │
│  │ (External)       │  │  /actuator/health             │ │
│  │                  │  │  Status checks every 30sec    │ │
│  │ SRV connection:  │  │                               │ │
│  │ mongodb+srv://   │  │  If fails >20min: stops app   │ │
│  │ user:pass@       │  │  If passes: keeps running    │ │
│  │ cluster.mongodb  │  │                               │ │
│  │ .net             │  │  Note: Render has no auto-    │ │
│  │                  │  │  pause like free tier         │ │
│  └──────────────────┘  └───────────────────────────────┘ │
│                                                               │
│  Public URL: https://chatnexus.onrender.com                 │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

---

## 🔄 Docker Image Build Process

```
┌─────────────────────────────────────────────────────────────────┐
│                  Docker Multi-Stage Build                       │
└─────────────────────────────────────────────────────────────────┘

STAGE 1: Build Stage
┌─────────────────────────────────────────────────────────────────┐
│   FROM maven:3.9-eclipse-temurin-17 AS builder                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✓ Copy pom.xml                                                │
│  ✓ Download Maven dependencies (cached)                        │
│  ✓ Copy src/ (source code)                                     │
│  ✓ Run: mvn clean package -DskipTests                          │
│  ✓ Output: target/ChatNexus-3.0.0.jar                          │
│  ✓ Size: ~400MB (includes build tools)                         │
│                                                                 │
│  [This stage is discarded in final image - saves space!]       │
└─────────────────────────────────────────────────────────────────┘

STAGE 2: Runtime Stage
┌─────────────────────────────────────────────────────────────────┐
│   FROM eclipse-temurin:17-jre-alpine                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✓ Base Image: Alpine JRE (minimal)                            │
│  ✓ Install curl (for health checks)                            │
│  ✓ Create appuser (non-root, security)                         │
│  ✓ COPY jar from builder stage (only jar!)                     │
│  ✓ Change ownership: chown appuser:appuser                     │
│  ✓ Switch user: USER appuser                                   │
│  ✓ Expose port: 8080                                           │
│  ✓ Health check: curl /actuator/health                         │
│  ✓ Run jar: java -jar app.jar                                  │
│                                                                 │
│  [Final image size: ~400-500MB total]                          │
│  [Builder stage discarded: saves ~200MB]                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

FINAL IMAGE
┌─────────────────────────────────────────────────────────────────┐
│  ChatNexus Docker Image                                         │
│  ├─ Base: Alpine JRE 17                                        │
│  ├─ App JAR: ChatNexus-3.0.0.jar                               │
│  ├─ User: appuser (non-root)                                   │
│  ├─ Port: 8080                                                 │
│  ├─ Health Check: /actuator/health                             │
│  └─ No source code (only compiled jar)                         │
│                                                                 │
│  Ready to deploy to Railway or Render ✓                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Environment Variable Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                  Environment Variables Flow                      │
└──────────────────────────────────────────────────────────────────┘

LOCAL DEVELOPMENT
┌─────────────────────────────────┐
│  .env (not in Git)              │
│  ├─ DEV_MONGO_URI               │
│  ├─ DEV_JWT_SECRET              │
│  └─ DEV_CLOUDINARY_*            │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  EnvConfig.java (loader)        │
│  Reads .env at startup          │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  System Properties              │
│  (set from .env)                │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  application-dev.properties     │
│  ├─ ${DEV_MONGO_URI}            │
│  ├─ ${DEV_JWT_SECRET}           │
│  └─ ${DEV_CLOUDINARY_*}         │
│                                 │
│  Spring resolves placeholders   │
└──────────┬──────────────────────┘
           │
           ▼
   Application runs ✓


RAILWAY PRODUCTION
┌─────────────────────────────────┐
│  Railway Dashboard              │
│  Environment Variables          │
│  ├─ PROD_MONGO_URI              │
│  ├─ PROD_JWT_SECRET             │
│  └─ PROD_CLOUDINARY_*           │
└──────────┬──────────────────────┘
           │
           │ (Set as Docker env vars)
           ▼
┌─────────────────────────────────┐
│  Running Container              │
│  Environment                    │
│  ├─ SPRING_PROFILES_ACTIVE=prod │
│  ├─ PROD_MONGO_URI              │
│  └─ PROD_CLOUDINARY_*           │
└──────────┬──────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  application-prod.properties    │
│  ├─ ${PROD_MONGO_URI}           │
│  ├─ ${PROD_JWT_SECRET}          │
│  └─ ${PROD_CLOUDINARY_*}        │
│                                 │
│  Spring resolves placeholders   │
└──────────┬──────────────────────┘
           │
           ▼
   Application runs ✓
```

---

## 📊 File Lifecycle in Git

```
┌──────────────────────────────────────────────────────────────┐
│              Version Control (Git) Tracking                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  COMMITTED TO GIT (tracked)           NOT IN GIT (.gitignore)
│  ─────────────────────────────────    ─────────────────────
│  ✓ Dockerfile                        ✗ .env
│  ✓ docker-compose.yml                ✗ .env.local
│  ✓ .dockerignore                     ✗ .env.*.local
│  ✓ railway.json                      ✗ .env.prod
│  ✓ .env.example (template)           ✗ Sensitive files
│  ✓ All documentation (*.md)
│  ✓ pom.xml (with dotenv dependency)
│  ✓ application.properties
│  ✓ application-dev.properties (uses ${placeholders})
│  ✓ application-prod.properties (uses ${placeholders})
│  ✓ src/ (source code)
│                                      Railway variables set via:
│                                      └─ Railway Dashboard UI
│                                      
│                                      Render variables set via:
│                                      └─ Render Dashboard UI
│                                      
│                                      Local .env file created by:
│                                      └─ Developer from .env.example
│                                      
└──────────────────────────────────────────────────────────────┘
```

---

## ✅ Deployment Readiness Checklist

```
┌─────────────────────────────────────────────────┐
│        Pre-Deployment Verification              │
├─────────────────────────────────────────────────┤
│                                                 │
│  Code & Git                                     │
│  ☐ All code committed to GitHub                │
│  ☐ Dockerfile present and tested               │
│  ☐ .dockerignore optimized                     │
│  ☐ docker-compose.yml for local testing        │
│                                                 │
│  Environment & Configuration                   │
│  ☐ .env created from .env.example              │
│  ☐ .env.example has all variables              │
│  ☐ .env is in .gitignore                       │
│  ☐ application.properties configured           │
│  ☐ application-dev.properties uses ${DEV_*}    │
│  ☐ application-prod.properties uses ${PROD_*}  │
│                                                 │
│  External Services                             │
│  ☐ MongoDB instance ready                      │
│  ☐ MongoDB URI obtained                        │
│  ☐ Cloudinary account created                  │
│  ☐ Cloudinary API keys obtained                │
│                                                 │
│  Local Testing                                 │
│  ☐ docker build succeeds                       │
│  ☐ docker-compose up works                     │
│  ☐ App accessible at localhost:8080            │
│  ☐ Health check passes                         │
│  ☐ DB connection successful                    │
│                                                 │
│  Platform Setup                                │
│  ☐ Railway/Render account created              │
│  ☐ GitHub connected to platform                │
│  ☐ All environment variables added             │
│  ☐ MongoDB configured/connected                │
│                                                 │
│  READY TO DEPLOY ✓                             │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 🎯 Quick Reference: Which URL to Use

```
DEVELOPMENT (Local)
├─ MongoDB: turntable.proxy.rlwy.net:16373
│            (Railway PUBLIC URL - visible to internet)
├─ Profile: dev
├─ JWT Expiration: 3600000 (1 hour)
└─ Database Access: Public, from laptop

PRODUCTION (Railway)
├─ MongoDB: mongodb.railway.internal:27017
│            (Railway INTERNAL URL - only works ON Railway)
├─ Profile: prod
├─ JWT Expiration: 86400000 (24 hours)
└─ Database Access: Internal network only

PRODUCTION (Render with Atlas)
├─ MongoDB: mongodb+srv://user:pass@cluster.mongodb.net
│            (MongoDB Atlas - internet-accessible)
├─ Profile: prod
├─ JWT Expiration: 86400000 (24 hours)
└─ Database Access: Public SRV connection
```

---

**This architecture ensures:**
- ✅ Secure credential management
- ✅ Easy local development and testing
- ✅ Seamless production deployment
- ✅ Scalable and maintainable setup
- ✅ Best practices for containerization

