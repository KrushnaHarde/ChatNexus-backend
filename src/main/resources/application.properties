# ================================
# ChatNexus Application Properties
# ================================
# Profile-specific configurations:
#   - application-dev.properties  ? Uses MONGO_PUBLIC_URL (for local development)
#   - application-prod.properties ? Uses MONGO_URL internal (for Railway deployment)

# Active Profile (default to dev for local development)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Application Info
spring.application.name=ChatNexus
info.app.name=ChatNexus
info.app.description=Real-time 1-to-1 chat application
info.app.version=3.0.0



